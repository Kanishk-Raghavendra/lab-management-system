# Lab Management System

## Project Setup
- Frontend: HTML, CSS, JavaScript
- Backend: Flask (to be set up later)
- Database: MySQL (to be set up later)

## Folder Structure
- `app/` → code for backend (models, routes, templates, static)
- `database/` → database scripts (schema, seed, queries)
- `docs/` → ER diagrams, requirements, roadmap
- `tests/` → test scripts